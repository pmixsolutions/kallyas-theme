# pmixsolutions kallyas theme

## Installation

Execute the following command in your terminal:

``` json
composer require pmixsolutions/kallyas-theme
```

You'll find the theme in the `Themes/` directory.
